import { RecentFirstPipe } from './recent-first.pipe';

describe('RecentFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new RecentFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
